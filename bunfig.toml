[run]
bun = true

[test]
preload = ["./tests/setup.ts"]

# Code coverage — always enabled (text + lcov)
# See: https://bun.sh/docs/test/coverage
coverage = true
coverageReporter = ["text", "lcov"]
coverageDir = "coverage"
coverageSkipTestFiles = true
