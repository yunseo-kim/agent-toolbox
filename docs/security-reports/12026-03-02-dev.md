# Agent Skills Security Scan Report

**Timestamp:** 2026-03-01T21:51:01.110125+00:00

## Summary

- **Total Skills Scanned:** 15
- **Safe Skills:** 11
- **Total Findings:** 93

### Findings by Severity

- **Critical:** 0
- **High:** 7
- **Medium:** 23
- **Low:** 38
- **Info:** 25

## Skill Results


---

### [FAIL] find-skills

- **Max Severity:** HIGH
- **Findings:** 7
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/find-skills

  #### [HIGH] Supply-chain risk: encourages installing skills from arbitrary GitHub/other sources via npx
  
  **Severity:** HIGH
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  **Location:** SKILL.md
  
  **Description:** The skill instructs the agent/user to discover and install additional skills using `npx skills add <owner/repo@skill>` and explicitly states installs can come from GitHub or other sources. This creates a supply-chain expansion path where an attacker can publish a malicious skill repo and have it installed/executed. The risk is amplified because the instructions do not include any provenance verification (publisher trust, commit pinning, signature verification, allowlists, review steps) before installation.
  
  **Code Snippet:**
  ```
  “npx skills add <package> - Install a skill from GitHub or other sources” and “Install with npx skills add <owner/repo@skill>”
  ```
  
  **Remediation:** Add explicit trust and verification steps before installation: require an allowlist of approved orgs/repos, prefer pinned commits/tags, require reviewing SKILL.md and scripts before enabling, and document signature/hash verification if supported. Recommend installing in a sandboxed environment and avoiding arbitrary third-party sources.

  #### [HIGH] Unsafe tool usage guidance: global install with auto-confirmation (-g -y) enables unattended changes
  
  **Severity:** HIGH
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill advises installing packages globally and skipping confirmation prompts (`-g -y`). This increases the blast radius (system/user-level changes) and reduces user friction/visibility, making it easier to unintentionally install malicious or unwanted skills and dependencies. This is a tool-abuse pattern because it encourages high-impact operations without explicit user confirmation or review.
  
  **Code Snippet:**
  ```
  “If the user wants to proceed, you can install the skill for them: npx skills add <owner/repo@skill> -g -y. The `-g` flag installs globally (user-level) and `-y` skips confirmation prompts.”
  ```
  
  **Remediation:** Remove `-y` from recommended commands; avoid global installs by default (prefer project-local or a dedicated skills directory). Require explicit user confirmation and present a review checklist (source, repo reputation, files to be executed, permissions/tools requested) before running install commands.

  #### [MEDIUM] Indirect prompt-injection exposure via external skill ecosystem content
  
  **Severity:** MEDIUM
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  **Location:** SKILL.md
  
  **Description:** The workflow relies on searching/browsing an external ecosystem (skills.sh and GitHub-hosted skills) and then installing content that contains instructions and executable scripts. While this package itself contains no scripts, it operationalizes a pipeline where untrusted external content can influence agent behavior after installation. Without guardrails (e.g., treat skill instructions as untrusted until reviewed), this increases susceptibility to instruction manipulation embedded in third-party skills.
  
  **Code Snippet:**
  ```
  “Browse skills at: https://skills.sh/” and instructions to install skills from GitHub/other sources
  ```
  
  **Remediation:** Add guidance to treat discovered skills as untrusted until reviewed; require manual inspection of SKILL.md and any scripts; recommend using a sandbox and limiting allowed-tools for newly installed skills; document a safe review process before enabling/using a new skill.

  #### [LOW] Missing/incorrect referenced files may indicate packaging issues or misleading documentation
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instructions reference files (Vercel.py, GitHub.py, the.py) that are not present. While not a direct exploit, missing references can confuse reviewers/users and may hide intended behavior or indicate incomplete packaging. It also complicates auditing because referenced components cannot be inspected.
  
  **Code Snippet:**
  ```
  Referenced Files: Vercel.py, GitHub.py, the.py (not found)
  ```
  
  **Remediation:** Remove nonexistent references or include the files. Ensure the skill package is self-consistent and auditable; document exactly what code/instructions are executed.

  #### [LOW] Manifest metadata omissions (license/compatibility/allowed-tools not specified)
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The manifest omits license, compatibility, and allowed-tools. Missing allowed-tools is optional per the spec, but it reduces clarity about intended tool usage and constraints, making it harder to enforce least privilege and audit expected behavior.
  
  **Code Snippet:**
  ```
  license: Not specified; compatibility: Not specified; allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit metadata: license, compatibility, and a least-privilege allowed-tools list (e.g., Bash only if truly required). Document any constraints and safety checks around installation.

  #### [LOW] Discovery/activation broadness: skill is designed to trigger on many generic user intents
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The 'When to Use' section is very broad (e.g., many generic “how do I do X” queries). This can lead to over-activation and unnecessary prompting to install external packages, increasing exposure to supply-chain and tool risks. This is not inherently malicious, but it is a capability/discovery pattern that can inflate usage beyond narrowly-scoped intent.
  
  **Code Snippet:**
  ```
  “Use this skill when the user: Asks ‘how do I do X’ … Asks ‘can you do X’ … Wants to search for tools, templates, or workflows …”
  ```
  
  **Remediation:** Narrow activation criteria (e.g., only when the user explicitly asks to find/install a skill). Add a decision gate: prefer solving directly unless the user requests installation or the task clearly requires a specialized skill.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] gitlab-ci-patterns

- **Max Severity:** MEDIUM
- **Findings:** 6
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/gitlab-ci-patterns

  #### [MEDIUM] Missing referenced files may enable tool/behavior substitution or unsafe fallbacks
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill’s instructions reference multiple internal template/markdown files (and a Python file) that are not present. In agent-skill contexts, missing referenced resources often lead to ad-hoc regeneration, downloading “equivalents” from the internet, or accepting user-provided replacements—each of which can become a vector for tool poisoning or indirect instruction manipulation if the agent is later guided to fetch/execute content from untrusted sources. While no explicit external fetch is instructed here, the package is incomplete and increases the likelihood of unsafe fallback behaviors during use.
  
  **Code Snippet:**
  ```
  Referenced files listed but not found: references/gitlab-ci.yml.template, assets/gitlab-ci.yml.template, templates/pipeline-stages.md, references/pipeline-stages.md, assets/pipeline-stages.md, templates/gitlab-ci.yml.template, GitLab.py
  ```
  
  **Remediation:** Bundle the referenced files within the skill package or remove/replace the references. If templates are to be generated, include deterministic generation steps and explicitly forbid downloading external templates or executing code from user-provided files. Add integrity checks (hashes) for any bundled templates.

  #### [LOW] Credential handling pattern may expose secrets via CI logs
  
  **Severity:** LOW
  **Category:** data_exfiltration
  **Rule ID:** LLM_DATA_EXFILTRATION
  **Location:** SKILL.md
  
  **Description:** The Docker login example uses `docker login -p $CI_REGISTRY_PASSWORD`, which can risk secret exposure in process listings or logs depending on runner configuration and shell tracing. While common, it is safer to use `--password-stdin` and ensure masking/protected variables are configured.
  
  **Code Snippet:**
  ```
  `docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY`
  ```
  
  **Remediation:** Use `echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"`. Ensure variables are masked/protected, disable shell debug (`set -x`), and scope credentials to least privilege.

  #### [LOW] Pipeline patterns include potentially high-impact operations without safety guardrails
  
  **Severity:** LOW
  **Category:** resource_abuse
  **Rule ID:** LLM_RESOURCE_ABUSE
  **Location:** SKILL.md
  
  **Description:** The provided GitLab CI examples include deployment and infrastructure-changing commands (e.g., kubectl apply/rollout and terraform apply -auto-approve). In real usage, these patterns can cause availability incidents or resource exhaustion if applied to the wrong environment/cluster or triggered unintentionally. This is not malicious by itself, but the skill lacks explicit guardrails (e.g., environment scoping, protected branches, approvals) beyond a few manual gates.
  
  **Code Snippet:**
  ```
  Examples: `kubectl apply -f k8s/` and `terraform apply -auto-approve tfplan`
  ```
  
  **Remediation:** Add explicit safety guidance: require protected environments/branches, enforce manual approvals for production, use least-privilege Kubernetes service accounts, add plan/apply separation with approvals, and avoid `-auto-approve` except in tightly controlled non-prod contexts.

  #### [LOW] Use of floating container image tags (latest) increases supply-chain risk
  
  **Severity:** LOW
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  **Location:** SKILL.md
  
  **Description:** Several examples use `:latest` tags (e.g., kubectl and trivy). Floating tags can change over time and introduce unreviewed behavior or compromised images into CI runs, which is a common supply-chain weakness in CI/CD configurations.
  
  **Code Snippet:**
  ```
  Examples: `image: bitnami/kubectl:latest`, `image: aquasec/trivy:latest`
  ```
  
  **Remediation:** Pin images to immutable digests (preferred) or specific version tags. Document a process for updating and verifying images (signatures/SBOMs) and restrict runner egress where possible.

  #### [LOW] allowed-tools and other manifest metadata not specified (reduced policy enforcement)
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The manifest omits `allowed-tools`, license, and compatibility. While optional, missing `allowed-tools` reduces the ability to enforce tool restrictions and can complicate governance and review of what the skill is permitted to do.
  
  **Code Snippet:**
  ```
  Manifest details show allowed-tools/license/compatibility not specified.
  ```
  
  **Remediation:** Add `allowed-tools` consistent with intended use (likely none or Read-only for a patterns-only skill), and specify license/compatibility to support governance and safe deployment.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] github-triage

- **Max Severity:** MEDIUM
- **Findings:** 7
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/github-triage

  #### [LOW] Over-broad/agentic capability claims and activation triggers without supporting instructions
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The skill description claims broad autonomous behavior: parallel/background tasks, reviewing PRs, and merging 'safe ones', with multiple trigger phrases. However, the SKILL.md instruction body is empty and key operational details (safety checks, confirmation gates, merge policy) are not specified. This can lead to capability inflation and unsafe activation/expectations (e.g., merging code without adequate review criteria).
  
  **Code Snippet:**
  ```
  Description: 'PRs: review bugfixes, merge safe ones. All parallel, all background. Triggers: ...' but Instruction Body is empty.
  ```
  
  **Remediation:** Add explicit, bounded instructions: require user confirmation before merges, define review/merge criteria, limit scope (repo/branch), and document any background/parallel behavior. Reduce trigger keywords to the minimum needed.

  #### [LOW] Missing manifest metadata (allowed-tools/license/compatibility) reduces enforceable safety boundaries
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The manifest omits allowed-tools, license, and compatibility. While allowed-tools is optional, its absence removes an important control for constraining tool usage (e.g., preventing unintended Write/Bash/network actions) and makes it harder to assess whether behavior matches declared permissions.
  
  **Code Snippet:**
  ```
  YAML Manifest Details: license/compatibility/allowed-tools not specified.
  ```
  
  **Remediation:** Specify allowed-tools with least privilege (e.g., Read/Grep/Glob unless Write/Bash/Python are truly required). Add license and compatibility fields for provenance and operational clarity.

  #### [LOW] Referenced files appear missing (broken references can mask intended logic or enable later substitution)
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill references codebase.py, determining.py, and the.py, but they are reported as not found. Missing referenced files can indicate incomplete packaging or an attempt to obscure where logic resides; it also creates an opportunity for later substitution (a different file with the same name) to change behavior without updating documentation.
  
  **Code Snippet:**
  ```
  Referenced Files: codebase.py, determining.py, the.py (not found).
  ```
  
  **Remediation:** Remove nonexistent references or include the files in the package. If they are required, pin their exact paths and document their purpose. Add integrity checks (e.g., hashes) if your distribution process supports it.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [INFO] 'SKILL.md (instruction body)' excluded from LLM analysis (15,347 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** SKILL.md (instruction body)
  
  **Description:** instruction body (15,347 chars) exceeds limit (15,000)
  
  **Remediation:** Increase llm_analysis.max_instruction_body_chars in your scan policy to include this content in LLM analysis.

  #### [MEDIUM] Unreferenced executable script present (potential hidden behavior/tool misuse)
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** scripts/gh_fetch.py
  
  **Description:** Static inventory indicates a Python script exists (scripts/gh_fetch.py) even though the provided skill description says no script files were found and the SKILL.md instruction body is empty. Unreferenced scripts can be invoked implicitly by an agent or later added to instructions, and they often contain the real operational logic (e.g., GitHub API access, token handling, filesystem reads). This mismatch increases the risk of hidden capabilities, unauthorized tool use, or data handling that is not disclosed in the manifest/instructions.
  
  **Code Snippet:**
  ```
  Pre-Scan Context: unreferenced_scripts: ['scripts/gh_fetch.py'] while 'No script files found' in provided summary.
  ```
  
  **Remediation:** 1) Review scripts/gh_fetch.py for network calls, credential access, command execution, and file reads/writes. 2) Ensure SKILL.md explicitly documents when/why the script is run and what data it accesses. 3) Add/declare allowed-tools consistent with actual behavior (e.g., Bash/Python/Read/Write) and restrict to minimum necessary. 4) Consider removing the script if unused.

  #### [INFO] 'scripts/gh_fetch.py' excluded from LLM analysis (12,447 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** scripts/gh_fetch.py
  
  **Description:** file size (12,447 chars) exceeds per-file limit (10,000)
  
  **Remediation:** Increase llm_analysis.max_code_file_chars in your scan policy to include this content in LLM analysis.


---

### [FAIL] skill-creator

- **Max Severity:** HIGH
- **Findings:** 5
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/skill-creator

  #### [HIGH] Unreferenced script suggests external content fetching risk (potential indirect prompt injection vector)
  
  **Severity:** HIGH
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  
  **Description:** Static inventory indicates an unreferenced script `scripts/gh_fetch.py` exists but was not provided for review. A file named `gh_fetch` strongly suggests GitHub/network fetching of remote content. If this skill fetches and then uses remote markdown/templates/instructions, it can create an indirect prompt injection path (external content influencing agent behavior) and potentially a supply-chain-like trust issue. Because the script content is missing, the risk cannot be ruled out and should be treated as a serious review gap.
  
  **Code Snippet:**
  ```
  Pre-Scan Context: unreferenced_scripts=['scripts/gh_fetch.py'] (content not provided)
  ```
  
  **Remediation:** Include `scripts/gh_fetch.py` in the package review and restrict/validate any remote content ingestion. If remote fetching is required: pin to specific commits/tags, verify checksums/signatures, treat fetched text as untrusted data (never as instructions), and add explicit allowlists for domains/paths.

  #### [INFO] 'SKILL.md (instruction body)' excluded from LLM analysis (17,631 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** SKILL.md (instruction body)
  
  **Description:** instruction body (17,631 chars) exceeds limit (15,000)
  
  **Remediation:** Increase llm_analysis.max_instruction_body_chars in your scan policy to include this content in LLM analysis.

  #### [INFO] 'scripts/init_skill.py' excluded from LLM analysis (10,798 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** scripts/init_skill.py
  
  **Description:** file size (10,798 chars) exceeds per-file limit (10,000)
  
  **Remediation:** Increase llm_analysis.max_code_file_chars in your scan policy to include this content in LLM analysis.

  #### [MEDIUM] Packaging script may include unintended files due to broad zipping scope and arcname calculation
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** scripts/package_skill.py
  
  **Description:** `scripts/package_skill.py` walks `skill_path.rglob('*')` and writes every file into the zip. The `arcname` is computed as `file_path.relative_to(skill_path.parent)`, which will embed the parent directory name and could unintentionally include a broader directory structure than expected in the resulting archive. While not a direct exploit by itself, this increases the chance of accidentally packaging sensitive or irrelevant files if the skill directory is mis-specified or contains unexpected content (e.g., secrets, build artifacts).
  
  **Code Snippet:**
  ```
  for file_path in skill_path.rglob('*'):
      ...
      arcname = file_path.relative_to(skill_path.parent)
      zipf.write(file_path, arcname)
  ```
  
  **Remediation:** Compute `arcname` relative to `skill_path` (not its parent), and implement an explicit allowlist (e.g., SKILL.md, scripts/, templates/, assets/, references/) plus a denylist for secrets (e.g., .env, *.key, id_rsa, credentials files). Consider requiring user confirmation of included files before writing the archive.

  #### [LOW] Potential compute/memory exhaustion when reading very large SKILL.md
  
  **Severity:** LOW
  **Category:** resource_abuse
  **Rule ID:** LLM_RESOURCE_ABUSE
  **Location:** scripts/quick_validate.py
  
  **Description:** `quick_validate.py` reads the entire `SKILL.md` into memory (`read_text()`) and uses a regex over the full content. In typical use this is fine, but a maliciously large SKILL.md could cause elevated memory/CPU usage during validation (a minor DoS vector).
  
  **Code Snippet:**
  ```
  content = skill_md.read_text()
  match = re.match(r'^---\n(.*?)\n---', content, re.DOTALL)
  ```
  
  **Remediation:** Add a maximum file size check before reading/parsing (e.g., refuse >1–5MB), and consider streaming/frontmatter-only parsing (read until the second '---').


---

### [OK] secrets-management

- **Max Severity:** MEDIUM
- **Findings:** 5
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/secrets-management

  #### [MEDIUM] Unreferenced Python script present (potential hidden behavior outside documented workflow)
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** Static inventory indicates a Python script (scripts/gh_fetch.py) exists but is not referenced anywhere in SKILL.md. Unreferenced executables are a common place to hide functionality that bypasses the documented intent of the skill (e.g., unexpected network access, data collection, or tool use). Without the script contents, its behavior cannot be validated against the manifest and instructions.
  
  **Code Snippet:**
  ```
  Pre-Scan Context: unreferenced_scripts: ['scripts/gh_fetch.py']
  ```
  
  **Remediation:** 1) Ensure every executable script is explicitly referenced and explained in SKILL.md (what it does, inputs/outputs, and when it runs). 2) Review scripts/gh_fetch.py for network calls, credential access, filesystem traversal, subprocess usage, and secret handling; remove if unnecessary. 3) Add a minimal allowlist of intended operations and require user confirmation before any network or secret-related actions.

  #### [LOW] Documentation includes hardcoded example secrets and insecure dev-mode Vault token usage that could be copied into real environments
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instructions include example credentials (e.g., password=secret, super-secret-password) and demonstrate running Vault in dev mode with VAULT_TOKEN='root'. Although presented as examples, these patterns are frequently copy-pasted into real CI/CD configurations, increasing the risk of weak secrets and insecure Vault deployments. Additionally, some examples echo potentially sensitive values to logs (e.g., printing API keys), which can lead to accidental exposure if used verbatim.
  
  **Code Snippet:**
  ```
  Examples include: export VAULT_TOKEN='root'; vault kv put ... password=secret; aws secretsmanager create-secret --secret-string "super-secret-password"; GitHub Secrets example echoes "API Key: ${{ secrets.API_KEY }}"
  ```
  
  **Remediation:** 1) Replace example secrets with clearly non-secret placeholders (e.g., 'REPLACE_ME') and add warnings not to use dev mode in production. 2) Avoid examples that print secrets; demonstrate safe usage patterns (masking, redaction, passing via env vars without echo). 3) Add a prominent 'Do not copy-paste to production' note for dev-mode snippets.

  #### [LOW] Missing/unspecified manifest metadata (allowed-tools, license, compatibility) reduces enforceable constraints
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The skill omits license, compatibility, and allowed-tools. While allowed-tools is optional, leaving it unspecified removes an important control for constraining tool usage (e.g., preventing unintended Bash/Python execution or file writes) and makes it harder to detect violations. Missing license/compatibility also reduces provenance and operational clarity.
  
  **Code Snippet:**
  ```
  YAML Manifest Details: license: Not specified; compatibility: Not specified; allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit metadata: (a) license and compatibility, (b) allowed-tools set to the minimum required (e.g., [Read] for documentation-only skills). If the skill is intended to be documentation-only, state that no scripts should be executed and remove unused scripts.

  #### [LOW] Referenced files are missing/inconsistent, increasing risk of users sourcing content externally
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** SKILL.md references multiple local files (e.g., references/vault-setup.md, references/github-secrets.md), but the provided package indicates these files are not found. When internal references are missing, users may fetch similarly named content from the internet or other untrusted sources, increasing the chance of indirect prompt injection or unsafe configuration guidance being introduced into workflows.
  
  **Code Snippet:**
  ```
  Referenced files listed as not found: references/vault-setup.md, references/github-secrets.md, templates/*, assets/*, AWS.py, Vault.py
  ```
  
  **Remediation:** Bundle the referenced files in the package and ensure paths match exactly, or remove/replace references. If external links are necessary, add guidance to verify provenance and do not treat external content as authoritative instructions.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] docs-changelog

- **Max Severity:** MEDIUM
- **Findings:** 6
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/docs-changelog

  #### [LOW] Multiple referenced files are missing, increasing risk of incorrect behavior or ad-hoc substitutions
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instructions reference numerous templates/examples that are not present in the provided package (e.g., assets/*, templates/*, and a .gemini path). This is primarily a reliability issue, but it can become a security concern if an agent compensates by fetching external content or following similarly named files elsewhere in the repo without validation.
  
  **Code Snippet:**
  ```
  Referenced but not found: "URL", "assets/highlights_examples.md", "templates/index_template.md", "assets/latest_template.md", ".gemini/skills/docs-changelog/references/highlights_examples.md", etc.
  ```
  
  **Remediation:** Bundle all referenced templates/examples within the skill package and update paths to be consistent. Explicitly forbid fetching templates from the network as a fallback.

  #### [LOW] Missing manifest metadata (allowed-tools/license/compatibility) reduces enforceability and review clarity
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The manifest omits `allowed-tools`, `license`, and `compatibility`. Missing `allowed-tools` is optional per spec, but it weakens policy enforcement and makes it harder to detect tool-scope violations (e.g., file writes, shell execution) during runtime governance.
  
  **Code Snippet:**
  ```
  "license: Not specified"; "compatibility: Not specified"; "allowed-tools: Not specified"
  ```
  
  **Remediation:** Add explicit `allowed-tools` (likely Read/Write and possibly Bash if formatting is required), plus license and compatibility fields to improve governance and auditability.

  #### [LOW] Skill instructions require running external formatter command (bun) without explicit safety/confirmation guidance
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The SKILL.md instructs the agent/user to run `bun run format` as a final step. While this is common in repos, it is an external tool execution step that could run arbitrary scripts defined in package.json. The skill provides no guardrails (e.g., confirm working directory, review package scripts, avoid running in untrusted repos). This is a tool-execution risk rather than a direct exploit in the skill itself, but it can be abused if the skill is used in a compromised repository.
  
  **Code Snippet:**
  ```
  "After making changes, run `bun run format` to ensure consistency."
  ```
  
  **Remediation:** Add a safety note to verify the repository is trusted and to inspect the `format` script in package.json before running. Consider making the step optional or requiring explicit user confirmation before executing shell commands.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [MEDIUM] File extension does not match actual content type
  
  **Severity:** MEDIUM
  **Category:** obfuscation
  **Rule ID:** FILE_MAGIC_MISMATCH
  **Location:** references/latest_template.md
  
  **Description:** File 'latest_template.md' extension (.md) suggests one format but Magika detected a different text format: Handlebars source (handlebars). This may indicate content obfuscation or a misnamed file.
  
  **Remediation:** Rename the file to match its actual content type, or remove it if it appears malicious.

  #### [MEDIUM] File extension does not match actual content type
  
  **Severity:** MEDIUM
  **Category:** obfuscation
  **Rule ID:** FILE_MAGIC_MISMATCH
  **Location:** references/preview_template.md
  
  **Description:** File 'preview_template.md' extension (.md) suggests one format but Magika detected a different text format: Handlebars source (handlebars). This may indicate content obfuscation or a misnamed file.
  
  **Remediation:** Rename the file to match its actual content type, or remove it if it appears malicious.


---

### [OK] javascript-testing-patterns

- **Max Severity:** LOW
- **Findings:** 5
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/javascript-testing-patterns

  #### [LOW] File type mismatch flagged for referenced templates (content/extension inconsistency)
  
  **Severity:** LOW
  **Category:** obfuscation
  **Rule ID:** LLM_OBFUSCATION
  
  **Description:** Static analysis indicates file type mismatches where file extensions do not match detected content for references/latest_template.md and references/preview_template.md. Content/extension mismatches can be used to hide executable instructions or payloads in files that appear benign, and they complicate review and downstream parsing assumptions.
  
  **Code Snippet:**
  ```
  Pre-Scan Context: File type mismatches (extension != content): references/latest_template.md, references/preview_template.md
  ```
  
  **Remediation:** Verify the actual content of the mismatched files and rename them to the correct extension/type. If they are intended to be markdown, ensure they contain only markdown and no embedded executable content; otherwise, document their true format and how they are safely consumed.

  #### [LOW] Referenced script files are missing (potential tool/behavior ambiguity)
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The SKILL.md references Python files (request.py, nodemailer.py, type.py) that are not present in the provided package. Missing referenced artifacts can cause agents to improvise replacements, fetch external equivalents, or otherwise deviate from intended behavior, increasing the risk of unsafe tool use or unreviewed code paths during execution.
  
  **Code Snippet:**
  ```
  Referenced Files: request.py (not found), nodemailer.py (not found), type.py (not found)
  ```
  
  **Remediation:** Remove the references if they are not required, or include the referenced files in the package. If the skill expects the agent to create them, explicitly document safe, minimal implementations and prohibit downloading code from external sources.

  #### [LOW] Missing allowed-tools metadata reduces enforceable tool constraints
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The manifest does not specify allowed-tools. While optional, the absence of explicit tool constraints reduces the ability to enforce least-privilege execution expectations for the skill and makes it harder to detect/flag tool misuse relative to declared intent.
  
  **Code Snippet:**
  ```
  YAML Manifest Details: allowed-tools: Not specified
  ```
  
  **Remediation:** Add an allowed-tools list consistent with intended usage (e.g., [Read, Write] if only editing tests; avoid Bash/Python unless required).

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [INFO] 'SKILL.md (instruction body)' excluded from LLM analysis (25,600 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** SKILL.md (instruction body)
  
  **Description:** instruction body (25,600 chars) exceeds limit (15,000)
  
  **Remediation:** Increase llm_analysis.max_instruction_body_chars in your scan policy to include this content in LLM analysis.


---

### [OK] create-pr

- **Max Severity:** MEDIUM
- **Findings:** 4
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/create-pr

  #### [MEDIUM] Potential command/argument injection risk via interpolated PR title/body in gh CLI example
  
  **Severity:** MEDIUM
  **Category:** command_injection
  **Rule ID:** LLM_COMMAND_INJECTION
  **Location:** SKILL.md
  
  **Description:** The gh CLI command example embeds user-controlled fields (type/scope/summary) into a shell command line. If an agent naively substitutes untrusted user input into the quoted `--title` argument (or modifies quoting), it can lead to shell metacharacter injection or unintended command behavior. The provided example uses quotes, which helps, but agent implementations often construct commands dynamically; without strict escaping/validation, this pattern is a common injection footgun.
  
  **Code Snippet:**
  ```
  Example:
  `gh pr create --draft --title "<type>(<scope>): <summary>" --body "$(cat <<'EOF' ... EOF )"`
  ```
  
  **Remediation:** Require strict validation of `<type>` against the enumerated set and sanitize/escape `<scope>` and `<summary>` for shell safety. Prefer passing arguments without shell interpolation (e.g., using a tool API or a subprocess call with an argv array) and avoid constructing commands via string concatenation.

  #### [MEDIUM] Skill instructions require Bash/GitHub CLI execution but allowed-tools is unspecified (tooling scope not constrained)
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill’s SKILL.md instructs running multiple Bash commands (git status/diff/log, git push, gh pr create) that can modify remote state (pushing branches, creating PRs). However, the manifest does not specify allowed-tools, so there is no explicit constraint or expectation-setting around tool usage. While missing allowed-tools is typically LOW/informational, in this case the instructions clearly drive potentially impactful tool actions (networked GitHub operations via gh), increasing the risk of unintended execution in automated agent contexts.
  
  **Code Snippet:**
  ```
  Instructions include:
  - `git push -u origin HEAD`
  - `gh pr create --draft --title ... --body ...`
  ```
  
  **Remediation:** Add an explicit `allowed-tools` list in YAML (e.g., `[Bash]` and any required file tools) and document a confirmation step before any remote-modifying commands (push/PR creation). Consider adding guidance to verify the remote/branch and to avoid running if not authenticated to the intended GitHub org.

  #### [LOW] File type mismatch flagged by pre-scan may indicate content/extension inconsistency
  
  **Severity:** LOW
  **Category:** obfuscation
  **Rule ID:** LLM_OBFUSCATION
  **Location:** SKILL.md
  
  **Description:** Static pre-scan reports file type mismatches for `references/latest_template.md` and `references/preview_template.md` (extension != content). While no referenced files are listed in the provided SKILL.md excerpt, mismatches can be used to hide non-markdown payloads or confuse downstream tooling that assumes markdown. This is a weak signal without the actual file contents, but worth verifying.
  
  **Code Snippet:**
  ```
  Pre-scan context: "File type mismatches (extension != content): references/latest_template.md, references/preview_template.md"
  ```
  
  **Remediation:** Inspect the mismatched files to confirm they are benign markdown. If they contain non-markdown data, rename with the correct extension and ensure no tooling executes or interprets them as code.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] docs-writer

- **Max Severity:** LOW
- **Findings:** 4
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/docs-writer

  #### [LOW] File type mismatch flags in static pre-scan could indicate content smuggling/obfuscation risk
  
  **Severity:** LOW
  **Category:** obfuscation
  **Rule ID:** LLM_OBFUSCATION
  
  **Description:** The pre-scan reports file type mismatches where extension does not match content for `references/latest_template.md` and `references/preview_template.md`. Even though their contents were not provided here, mismatches can be used to hide non-markdown payloads or confuse downstream tooling that assumes markdown safety. In an agent skill context, this can become a detection-evasion vector if later tooling renders/executes embedded content differently than expected.
  
  **Code Snippet:**
  ```
  Pre-Scan Context: “File type mismatches (extension != content): references/latest_template.md, references/preview_template.md”
  ```
  
  **Remediation:** Inspect the two files and ensure their content matches the declared type. If they contain non-markdown (e.g., HTML, scripts, binary blobs), rename appropriately, remove executable content, and add repository checks to prevent extension/content mismatches.

  #### [LOW] Over-broad activation guidance may cause unintended use across repositories
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The description and instructions position this as the “default docs skill” for many doc-related tasks across repositories. While not inherently malicious, broad activation language can increase the chance of the skill being selected in contexts where it is not appropriate, which is a mild capability-inflation/discovery-abuse risk. No explicit keyword baiting or impersonation is present, and there are no scripts, so impact is limited.
  
  **Code Snippet:**
  ```
  “Use this as the default docs skill…” and description: “Unified documentation workflow… across repositories.”
  ```
  
  **Remediation:** Tighten activation criteria: specify clearer triggers (e.g., only when user requests documentation edits/reviews) and add explicit non-goals (e.g., not for code execution, not for security review, not for deployment/runbook operations).

  #### [LOW] Inconsistent/invalid referenced file paths may enable tool misuse or confusion during execution
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** SKILL.md states that all profile guidance is self-contained under `references/`, but the provided referenced-file list includes multiple paths outside `references/` (e.g., `assets/...`, `templates/...`) and even a non-existent `that.py`. Several of these are explicitly marked “not found”. In an agentic setting, such inconsistencies can cause the agent to search broadly for similarly named files elsewhere in the repo, increasing the chance of accidentally ingesting untrusted or attacker-controlled content (e.g., a malicious `assets/PROFILE-NEXTJS.md` placed in the target repository). While no explicit instruction says to search outside the skill package, the mismatch between declared boundaries and references is a reliability and potential tool-safety concern.
  
  **Code Snippet:**
  ```
  SKILL.md references only `references/PROFILE-*.md` and `references/TEMPLATES.md`, but the referenced-file inventory includes `assets/PROFILE-NEXTJS.md` (not found), `templates/TEMPLATES.md` (not found), and `that.py` (not found).
  ```
  
  **Remediation:** Normalize and validate references: (1) ensure all referenced files exist within the skill package, (2) remove stray references (`assets/`, `templates/`, `that.py`) or update SKILL.md to the correct paths, (3) add a rule: do not search the user’s repository for missing skill reference files; fail closed and ask for remediation if a referenced file is missing.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] github-actions-templates

- **Max Severity:** MEDIUM
- **Findings:** 4
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/github-actions-templates

  #### [MEDIUM] Use of unpinned third-party GitHub Actions (supply chain risk)
  
  **Severity:** MEDIUM
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  **Location:** SKILL.md
  
  **Description:** The provided workflow templates reference multiple third-party GitHub Actions by floating tags (e.g., @master, @v1/@v2/@v3/@v4/@v5). While major-version tags are common, they are still mutable and can be retagged; @master is especially risky. This creates a supply-chain exposure where a compromised upstream action or tag movement could execute attacker-controlled code in CI, potentially accessing repository contents and secrets available to the workflow.
  
  **Code Snippet:**
  ```
  uses: aquasecurity/trivy-action@master
  uses: snyk/actions/node@master
  uses: slackapi/slack-github-action@v1
  uses: codecov/codecov-action@v3
  uses: docker/build-push-action@v5
  ```
  
  **Remediation:** Pin all third-party actions to immutable commit SHAs (preferred) or at least to full semantic versions where available. Avoid @master/@main. Add periodic dependency review for action updates and consider allowlisting trusted publishers.

  #### [LOW] Referenced template files missing / inconsistent references may enable confusion or substitution
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The instructions reference multiple internal files (assets/, references/, templates/) that are reported as not found, and the pre-scan notes indicate file type mismatches. Missing or inconsistent references can lead to users/agents substituting content from elsewhere (e.g., downloading templates from the internet), increasing the chance of capability inflation or accidental adoption of untrusted workflow code.
  
  **Code Snippet:**
  ```
  References: assets/test-workflow.yml, assets/deploy-workflow.yml, assets/matrix-build.yml, references/common-workflows.md (all reported not found)
  Pre-scan: File type mismatches: references/latest_template.md, references/preview_template.md
  ```
  
  **Remediation:** Ensure all referenced files exist within the package and match their stated purpose/content type. If external sources are intended, explicitly warn that external content is untrusted and should be reviewed before use.

  #### [LOW] Missing allowed-tools manifest field reduces enforceable tool restrictions
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill manifest does not specify allowed-tools. While optional, the absence of explicit tool restrictions reduces the ability to enforce least-privilege expectations for any future evolution of the skill (e.g., if scripts are later added).
  
  **Code Snippet:**
  ```
  allowed-tools: Not specified
  ```
  
  **Remediation:** Add an explicit allowed-tools list consistent with intended usage (likely Read/Write for template generation, and avoid Bash/Python unless required).

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [FAIL] mcp-builder

- **Max Severity:** HIGH
- **Findings:** 8
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/mcp-builder

  #### [LOW] Missing/invalid referenced files may indicate packaging integrity issues
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  
  **Description:** The instructions reference `its.py` and `concise.py`, but they are not found. While not inherently malicious, missing references can be used to confuse reviewers, hide intended behavior, or cause agents to fetch substitutes from elsewhere. It also reduces confidence in package integrity and reproducibility.
  
  **Code Snippet:**
  ```
  Referenced files list includes: `its.py` (not found), `concise.py` (not found).
  ```
  
  **Remediation:** Remove nonexistent references or include the files. If they are meant to be external, explicitly mark them as untrusted and do not instruct agents to fetch/execute them.

  #### [INFO] './reference/evaluation.md' excluded from LLM analysis (21,659 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** ./reference/evaluation.md
  
  **Description:** file size (21,659 chars) exceeds per-file limit (8,000)
  
  **Remediation:** Increase llm_analysis.max_referenced_file_chars in your scan policy to include this content in LLM analysis.

  #### [INFO] './reference/node_mcp_server.md' excluded from LLM analysis (28,472 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** ./reference/node_mcp_server.md
  
  **Description:** file size (28,472 chars) exceeds per-file limit (8,000)
  
  **Remediation:** Increase llm_analysis.max_referenced_file_chars in your scan policy to include this content in LLM analysis.

  #### [INFO] './reference/python_mcp_server.md' excluded from LLM analysis (25,099 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** ./reference/python_mcp_server.md
  
  **Description:** file size (25,099 chars) exceeds per-file limit (8,000)
  
  **Remediation:** Increase llm_analysis.max_referenced_file_chars in your scan policy to include this content in LLM analysis.

  #### [HIGH] Instructions encourage loading and trusting external web content (indirect prompt-injection surface)
  
  **Severity:** HIGH
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  **Location:** SKILL.md
  
  **Description:** The skill explicitly instructs the agent to fetch multiple external URLs (modelcontextprotocol.io sitemap/spec pages and raw GitHub READMEs) as part of the workflow. If an agent follows these instructions and then treats fetched content as authoritative instructions (common failure mode), a malicious or compromised upstream page could inject tool-using directives (e.g., exfiltrate files, run commands, install packages). This is a classic transitive trust / indirect prompt injection risk because the skill normalizes pulling remote markdown and using it during execution without specifying any sanitization, allowlisting, or “treat as data only” constraints.
  
  **Code Snippet:**
  ```
  Examples in instructions:
  - "Start with the sitemap ... https://modelcontextprotocol.io/sitemap.xml"
  - "Then fetch specific pages with .md suffix ... https://modelcontextprotocol.io/specification/draft.md"
  - "Use WebFetch to load https://raw.githubusercontent.com/modelcontextprotocol/typescript-sdk/main/README.md" (and python-sdk README)
  - "Use web search and WebFetch as needed."
  ```
  
  **Remediation:** Add explicit guardrails: (1) treat all fetched content as untrusted reference material, not executable instructions; (2) restrict fetching to an allowlist of domains and pinned commit SHAs for GitHub raw content; (3) require human confirmation before acting on any instructions found in fetched pages; (4) strip/ignore imperative language from fetched docs and only extract specific factual sections.

  #### [HIGH] Potential command execution risk via stdio transport when command/args are derived from untrusted input
  
  **Severity:** HIGH
  **Category:** command_injection
  **Rule ID:** LLM_COMMAND_INJECTION
  **Location:** scripts/connections.py
  
  **Description:** The provided connection helper supports launching an MCP server via stdio by accepting an arbitrary `command` and `args`. While this file itself does not execute a shell directly, it enables spawning arbitrary processes if upstream code passes user-controlled values into `create_connection(..., transport='stdio', command=..., args=...)`. In agent settings, it is common for tool arguments to be influenced by user prompts; without strict allowlisting, this becomes an arbitrary command execution vector (e.g., running unexpected binaries, invoking interpreters with attacker-controlled scripts).
  
  **Code Snippet:**
  ```
  MCPConnectionStdio stores arbitrary command/args and passes them into StdioServerParameters:
  - `def __init__(self, command: str, args: list[str] = None, env: dict[str, str] = None)`
  - `return stdio_client(StdioServerParameters(command=self.command, args=self.args, env=self.env))`
  - `create_connection(...): if transport == 'stdio': return MCPConnectionStdio(command=command, args=args, env=env)`
  ```
  
  **Remediation:** Constrain stdio launching: (1) require `command` to be selected from a fixed allowlist of known MCP server executables; (2) validate/normalize `args` (no file paths outside expected directories, no interpreter flags that load arbitrary code); (3) disallow passing arbitrary `env` from untrusted sources; (4) document that `command/args/env` must never be user-controlled and enforce this at the API boundary.

  #### [MEDIUM] Tooling/behavior mismatch risk: skill is a 'guide' but includes executable connection code enabling network access and subprocess interaction
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** scripts/connections.py
  
  **Description:** The manifest describes a development guide, but the package includes executable code (`scripts/connections.py`) that can connect to remote MCP servers over HTTP/SSE and can spawn local stdio servers. This expands the operational capability beyond documentation and increases the chance an agent will use it as an automation utility. Without explicit allowed-tools declarations and without clear user confirmation requirements, this can lead to unintended tool use (network connections, subprocess spawning) in environments that expected a read-only guide.
  
  **Code Snippet:**
  ```
  Package includes network-capable clients:
  - `from mcp.client.sse import sse_client`
  - `from mcp.client.streamable_http import streamablehttp_client`
  And stdio subprocess launching via `StdioServerParameters(command=..., args=..., env=...)`.
  Manifest: `allowed-tools: Not specified`.
  ```
  
  **Remediation:** Clarify intent and constrain execution: (1) add `allowed-tools` in SKILL.md frontmatter consistent with expected use; (2) explicitly state when/why scripts should be run and require confirmation before connecting to remote URLs or launching subprocesses; (3) consider separating 'guide' content from 'automation utilities' into different skills/packages.

  #### [INFO] 'scripts/evaluation.py' excluded from LLM analysis (12,559 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** scripts/evaluation.py
  
  **Description:** file size (12,559 chars) exceeds per-file limit (10,000)
  
  **Remediation:** Increase llm_analysis.max_code_file_chars in your scan policy to include this content in LLM analysis.


---

### [OK] dependency-upgrade

- **Max Severity:** MEDIUM
- **Findings:** 6
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/dependency-upgrade

  #### [LOW] Manifest metadata omissions reduce enforceable tool restrictions
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  
  **Description:** The manifest omits `allowed-tools`, license, and compatibility. While `allowed-tools` is optional, its absence prevents policy-based enforcement of tool usage boundaries (e.g., preventing Bash execution or file writes) and makes it harder to assess whether the skill’s recommended commands align with intended permissions.
  
  **Code Snippet:**
  ```
  Manifest details show: license not specified; compatibility not specified; allowed-tools not specified.
  ```
  
  **Remediation:** Add explicit metadata:
- Specify `allowed-tools` consistent with intended use (likely includes Bash; possibly Read/Write if scripts are added).
- Provide license and compatibility notes.
- If the skill is documentation-only, state that no scripts are executed and that commands are examples requiring user confirmation.

  #### [MEDIUM] Indirect prompt-injection / transitive trust risk via remote code and content execution patterns
  
  **Severity:** MEDIUM
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  **Location:** SKILL.md
  
  **Description:** The skill instructions include workflows that fetch and execute/consume remote content (e.g., curl a changelog from GitHub; run jscodeshift transforms directly from a remote URL). While these are common developer practices, in an agent-skill context they create an indirect prompt-injection and supply-chain style risk: a compromised upstream URL or MITM could deliver malicious transforms or misleading instructions that the agent may follow. This is especially risky because the examples encourage running remote transforms against local source trees, which can introduce backdoors or destructive edits.
  
  **Code Snippet:**
  ```
  Examples include:
  - `curl https://raw.githubusercontent.com/facebook/react/master/CHANGELOG.md`
  - `npx jscodeshift -t https://raw.githubusercontent.com/reactjs/react-codemod/master/transforms/rename-unsafe-lifecycles.js src/`
  ```
  
  **Remediation:** Prefer pinned, verified sources and local copies:
- Vendor codemods into the repo (or the skill package) and review them before execution.
- Pin to immutable commit SHAs rather than branch heads (e.g., `.../raw/<commit>/...`).
- Add guidance to verify checksums/signatures and to run in `--dry` mode first.
- Require explicit user confirmation before running any command that downloads remote content or applies automated code modifications.

  #### [MEDIUM] Supply-chain exposure through unpinned package execution (npx) and dependency update commands
  
  **Severity:** MEDIUM
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  **Location:** SKILL.md
  
  **Description:** The skill recommends using `npx` to run packages (e.g., npm-check-updates, madge, jscodeshift) and performing broad upgrades (`@latest`). In practice, `npx` may download and execute packages at run time, and `@latest` pulls whatever is current at execution time. This increases exposure to typosquatting, compromised packages, or unexpected breaking/malicious releases—particularly when executed automatically by an agent.
  
  **Code Snippet:**
  ```
  Examples include:
  - `npx npm-check-updates`
  - `npx madge --image graph.png src/`
  - `npm install typescript@latest`
  - `npm install package@latest`
  ```
  
  **Remediation:** Harden dependency execution:
- Pin versions for tooling (e.g., `npx npm-check-updates@<version>`), or install devDependencies with lockfiles and run via `npm run`.
- Prefer `npm ci` in CI and ensure lockfiles are committed.
- Add guidance to review package provenance, use npm audit carefully, and avoid unattended upgrades without review.

  #### [LOW] Package completeness/integrity issue: many referenced files are missing; unreferenced scripts exist
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The instructions reference numerous internal files (assets/references/templates) that are reported as not found, while static inventory indicates unreferenced scripts (`scripts/evaluation.py`, `scripts/connections.py`). Missing referenced files can lead to agents seeking substitutes from external sources (increasing indirect injection risk), and unreferenced scripts can hide unexpected capabilities that are not described in SKILL.md. Without reviewing those scripts, the package’s true behavior cannot be validated.
  
  **Code Snippet:**
  ```
  Referenced files listed as not found (e.g., `assets/compatibility-matrix.md`, `references/semver.md`, etc.). Pre-scan: `unreferenced_scripts`: ['scripts/evaluation.py', 'scripts/connections.py']
  ```
  
  **Remediation:** Improve integrity and reduce hidden-surface area:
- Include the referenced files or remove the references.
- Review and document any scripts shipped in the package; remove unused scripts.
- If scripts are intended to be used, explicitly reference them in SKILL.md and describe their behavior and required permissions.

  #### [LOW] Tooling/behavioral risk: instructions include file-modifying and branch-destructive commands without explicit safety gates
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill includes examples that modify many files (codemods, custom migration scripts) and a rollback script that deletes a branch (`git branch -D upgrade-branch`). In an agent setting, these patterns can cause unintended destructive changes if executed without confirmation, scoping, or dry-run safeguards. This is not inherently malicious, but it is a tool-use safety concern.
  
  **Code Snippet:**
  ```
  Examples include:
  - `fs.writeFileSync(file, content);` in the migration script example
  - `git branch -D upgrade-branch` in rollback.sh example
  ```
  
  **Remediation:** Add explicit guardrails:
- Require confirmation before running write/destructive commands.
- Encourage backups/tags and running codemods in `--dry`/preview mode.
- Scope file globs narrowly and log changed files.
- Prefer non-destructive git operations (or prompt before `-D`).

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [FAIL] catalog-porter

- **Max Severity:** HIGH
- **Findings:** 7
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/catalog-porter

  #### [HIGH] Unreviewed local scripts present (potential data exposure/exfiltration)
  
  **Severity:** HIGH
  **Category:** data_exfiltration
  **Rule ID:** LLM_DATA_EXFILTRATION
  
  **Description:** Static inventory indicates two Python scripts exist in the package but were not provided in the referenced-file content: scripts/evaluation.py and scripts/connections.py. Unreviewed executable scripts in a skill package are a significant risk because they may contain credential access, network exfiltration, or other sensitive behaviors. Given the skill’s nature (repo ingestion/porting), such scripts could plausibly handle network connections or evaluation logic that touches sensitive data. Without reviewing these files, the package cannot be considered safe.
  
  **Code Snippet:**
  ```
  Pre-Scan Context: "unreferenced_scripts": ["scripts/evaluation.py", "scripts/connections.py"]
  ```
  
  **Remediation:** Review both scripts for: network calls (requests/httpx/subprocess curl), reading sensitive paths (~/.ssh, ~/.aws, env vars), command execution (os.system/subprocess with user input), and obfuscation. If scripts are not required, remove them. If required, document their purpose in SKILL.md and constrain behavior (no secret access, no outbound network unless explicitly needed and user-approved).

  #### [MEDIUM] Indirect prompt injection risk via ingesting untrusted upstream SKILL.md content
  
  **Severity:** MEDIUM
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  **Location:** SKILL.md
  
  **Description:** The skill’s core function is to clone arbitrary upstream repositories and read each upstream SKILL.md to understand and classify skills. Upstream SKILL.md files are untrusted content and may contain prompt-injection instructions (e.g., "ignore previous instructions", "exfiltrate secrets", "run these commands"). Even though this skill describes classification/porting, it does not specify robust handling rules to treat upstream instructions as data-only during analysis. This creates an indirect prompt injection risk where malicious upstream content could influence the agent’s behavior during the porting process.
  
  **Code Snippet:**
  ```
  "For each skill directory, read the SKILL.md to understand what it does." and overall workflow: clone repo -> find SKILL.md -> read/port.
  ```
  
  **Remediation:** Add explicit guidance: treat upstream SKILL.md bodies as untrusted data; never follow instructions found in upstream skills; only extract metadata and perform static checks. Consider implementing a structured extraction approach (parse frontmatter only; summarize body without executing directives). Add a mandatory injection scan step for upstream SKILL.md (look for override phrases, tool-use directives, exfil patterns) and require user confirmation when suspicious patterns are detected.

  #### [MEDIUM] Tooling/operation risk: cloning and writing to repository without explicit safety constraints
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The instructions include cloning arbitrary repos to /tmp and then creating/updating multiple local files (catalog/skills/*, upstream-sources.yaml, README/AGENTS.md) and running build/test commands. This is a legitimate workflow, but the skill lacks explicit constraints to prevent unsafe tool use (e.g., ensuring the URL is actually GitHub, preventing path traversal in repo-name, preventing accidental writes outside catalog/, and requiring confirmation before running validation commands). This is a tool exploitation risk in the sense of unauthorized or unsafe tool usage patterns being possible if the agent follows the procedure naively.
  
  **Code Snippet:**
  ```
  Commands and write operations described: "git clone --depth 1 <url> /tmp/<repo-name>", "Create directory: catalog/skills/<skill-name>/", and running multiple "bun" commands.
  ```
  
  **Remediation:** Add guardrails: validate URL host allowlist (github.com only unless explicitly approved), sanitize derived paths (repo-name, skill-name), enforce a strict write allowlist (only under catalog/), and require explicit user approval before executing any shell commands or modifying tracked files. Prefer using a safe temp dir (mktemp -d) rather than interpolated /tmp/<repo-name>.

  #### [LOW] Potential compute/resource exhaustion from unbounded repo scanning and build/test steps
  
  **Severity:** LOW
  **Category:** resource_abuse
  **Rule ID:** LLM_RESOURCE_ABUSE
  **Location:** SKILL.md
  
  **Description:** The workflow can clone and scan large repositories and then run multiple build/validate/test steps (bun run build:index/validate/typecheck/test). Without limits (repo size, file count, timeouts), this can lead to resource exhaustion or long-running operations, especially if pointed at very large repos or repos with heavy test suites.
  
  **Code Snippet:**
  ```
  "git clone --depth 1 <url> ..." + "find ... -name SKILL.md" + "bun run ..." sequence.
  ```
  
  **Remediation:** Add limits and timeouts: cap repo size (or require user confirmation if above threshold), limit discovery to expected roots, add timeouts to commands, and allow a 'dry-run' mode that only performs analysis without running builds/tests.

  #### [LOW] Over-broad capability and activation surface (repo cloning/porting workflow)
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The skill is designed to accept arbitrary GitHub repo URLs, clone them, discover SKILL.md files, and then port content into the local catalog. This is a powerful, high-trust workflow that can be triggered by broad user intents ("port skills", "analyze repo", "add upstream source"). While not inherently malicious, it increases the chance of unintended activation and expands the operational scope (filesystem writes, repo ingestion) beyond a narrow, single-purpose action. This is a form of capability inflation/activation abuse risk because the skill positions itself as a general ingestion/porting pipeline that can be invoked in many contexts.
  
  **Code Snippet:**
  ```
  "Use when the user provides a GitHub repo URL to analyze and port skills from, or asks to 'port skills', 'analyze repo', 'add upstream source', or 'catalog porter'."
  ```
  
  **Remediation:** Narrow the invocation criteria and add explicit preconditions (e.g., require an explicit repo URL + explicit confirmation). Add a safety preflight checklist (confirm target repo, confirm destination paths, confirm no secrets in repo) before any clone/write operations.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [INFO] 'references/notice-templates.md' excluded from LLM analysis (13,928 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** references/notice-templates.md
  
  **Description:** file size (13,928 chars) exceeds per-file limit (8,000)
  
  **Remediation:** Increase llm_analysis.max_referenced_file_chars in your scan policy to include this content in LLM analysis.


---

### [OK] doc-coauthoring

- **Max Severity:** MEDIUM
- **Findings:** 5
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/doc-coauthoring

  #### [MEDIUM] Over-broad activation/trigger guidance may cause unintended invocation (capability inflation)
  
  **Severity:** MEDIUM
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The description includes broad trigger conditions (e.g., 'Trigger when user mentions writing docs, creating proposals, drafting specs, or similar documentation tasks'). This can cause the skill to activate in many contexts and may increase the chance of inappropriate or unintended use, especially if the skill later gains tool access or is combined with other skills.
  
  **Code Snippet:**
  ```
  Trigger when user mentions writing docs, creating proposals, drafting specs, or similar documentation tasks.
  ```
  
  **Remediation:** Narrow activation criteria to specific, unambiguous user intents and add explicit user confirmation steps before any sensitive actions (file access, external sharing, etc.).

  #### [MEDIUM] Referenced Python files missing (potential tool/script shadowing or incomplete package)
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill instructions reference Python modules (messaging.py, real.py) that are not present in the provided package snapshot. Missing referenced code can indicate an incomplete skill, but it can also be a sign of tool/script shadowing risk (e.g., the runtime might import similarly named modules from elsewhere on the system/PYTHONPATH) or a packaging inconsistency that could lead to unexpected behavior if the agent attempts to execute or import them.
  
  **Code Snippet:**
  ```
  Referenced Files: messaging.py (not found), real.py (not found)
  ```
  
  **Remediation:** Ensure all referenced files are included in the skill package and are imported via explicit, package-local paths. Avoid ambiguous imports; use a package namespace (e.g., skill_package.messaging) and validate file existence before execution.

  #### [LOW] Missing manifest metadata (allowed-tools/license/compatibility) reduces enforceability and reviewability
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The manifest omits allowed-tools, license, and compatibility. While allowed-tools is optional, its absence reduces the ability to enforce tool restrictions and makes it harder to assess expected execution boundaries. Missing license/compatibility also reduces supply-chain clarity and operational expectations.
  
  **Code Snippet:**
  ```
  license: Not specified; compatibility: Not specified; allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit allowed-tools (even if empty/read-only), specify license, and document compatibility/assumptions (e.g., no network, no file writes) to support governance and automated policy enforcement.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [INFO] 'SKILL.md (instruction body)' excluded from LLM analysis (15,341 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** SKILL.md (instruction body)
  
  **Description:** instruction body (15,341 chars) exceeds limit (15,000)
  
  **Remediation:** Increase llm_analysis.max_instruction_body_chars in your scan policy to include this content in LLM analysis.


---

### [OK] git-master

- **Max Severity:** MEDIUM
- **Findings:** 8
- **Directory:** /home/runner/work/awesome-agent-toolbox/awesome-agent-toolbox/.agents/skills/git-master

  #### [MEDIUM] Capability inflation / activation pressure in manifest description ("MUST USE for ANY git operations")
  
  **Severity:** MEDIUM
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  
  **Description:** The skill description attempts to force selection/priority ("MUST USE for ANY git operations", "STRONGLY RECOMMENDED") and includes a large trigger keyword list. This is consistent with discovery/activation abuse and capability inflation: it can bias an agent/router to invoke this skill broadly, even when unnecessary, increasing the attack surface and the chance of unintended tool use in downstream steps.
  
  **Code Snippet:**
  ```
  Description: "MUST USE for ANY git operations... STRONGLY RECOMMENDED... Triggers: 'commit', 'rebase', 'squash', ..."
  ```
  
  **Remediation:** Rewrite the description to be scoped and non-coercive (e.g., "Helps with advanced git history operations"). Remove/limit trigger keyword baiting and avoid language that claims priority over other skills.

  #### [LOW] References to multiple missing files (integrity/packaging issue; potential future instruction-manipulation risk)
  
  **Severity:** LOW
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  
  **Description:** The skill references several Python files that are not present. While not a direct exploit by itself, missing referenced artifacts can lead to unsafe fallback behaviors (e.g., fetching replacements externally, or prompting users to supply files), which increases exposure to indirect prompt injection or untrusted code substitution.
  
  **Code Snippet:**
  ```
  Referenced but not found: scratch.py, M.py, main.py, Phase.py, 8.py, git.py, multiple.py, changes.py
  ```
  
  **Remediation:** Remove invalid references or include the referenced files in the package. Explicitly prohibit downloading replacement scripts from external sources; require bundled, reviewed resources only.

  #### [LOW] Missing license/compatibility metadata
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  
  **Description:** License and compatibility fields are not specified. This is primarily a governance/operational risk (unclear usage rights and runtime expectations) rather than a direct security exploit, but it can hinder review and safe deployment decisions.
  
  **Code Snippet:**
  ```
  license: Not specified; compatibility: Not specified
  ```
  
  **Remediation:** Add explicit license and compatibility information; include versioning and a brief security note describing intended tool usage and constraints.

  #### [LOW] Missing allowed-tools declaration reduces enforceable tool-safety boundaries
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  
  **Description:** The manifest omits `allowed-tools`. While optional, the absence of explicit tool constraints makes it harder to enforce least-privilege expectations for a skill that is positioned as broadly applicable to git operations (which often implies Bash usage).
  
  **Code Snippet:**
  ```
  allowed-tools: Not specified
  ```
  
  **Remediation:** Add an `allowed-tools` list consistent with intended behavior (e.g., [Bash, Read, Grep, Glob] and Write only if necessary).

  #### [MEDIUM] Unreferenced executable scripts present (potential hidden behavior)
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** Static inventory indicates Python scripts exist under `scripts/` but are not referenced in the provided SKILL.md instructions. Unreferenced scripts can indicate hidden or unintended functionality that may be invoked by an agent/tooling implicitly or later added instructions, and should be reviewed for tool misuse, data access, or network activity.
  
  **Code Snippet:**
  ```
  Pre-Scan Context: unreferenced_scripts: ['scripts/evaluation.py', 'scripts/connections.py']
  ```
  
  **Remediation:** Review these scripts for sensitive operations (filesystem traversal, credential access, subprocess calls, network requests). Either (a) reference and document them explicitly in SKILL.md with clear, minimal behavior, or (b) remove them from the package if not required.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [MEDIUM] Bash: tainted data flows to dangerous sink
  
  **Severity:** MEDIUM
  **Category:** command_injection
  **Rule ID:** BEHAVIOR_BASH_TAINT_FLOW
  **Location:** SKILL.md:499
  
  **Description:** Variable $MERGE_BASE (line 8) flows to `sh` at line 9.
  
  **Code Snippet:**
  ```
  GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash $MERGE_BASE
  ```
  
  **Remediation:** Review the data flow from source to sink. Avoid sending sensitive or untrusted data to network or execution commands.

  #### [INFO] 'SKILL.md (instruction body)' excluded from LLM analysis (33,971 chars)
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** LLM_CONTEXT_BUDGET_EXCEEDED
  **Location:** SKILL.md (instruction body)
  
  **Description:** instruction body (33,971 chars) exceeds limit (15,000)
  
  **Remediation:** Increase llm_analysis.max_instruction_body_chars in your scan policy to include this content in LLM analysis.


---

### Cross-Skill Findings

- **Findings:** 6

  #### [HIGH] Potential data relay attack pattern detected
  
  **Severity:** HIGH
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_DATA_RELAY
  **Location:** (cross-skill analysis)
  
  **Description:** Skills appear to form a data relay chain. Collectors (gitlab-ci-patterns, github-triage, skill-creator, secrets-management, docs-changelog, javascript-testing-patterns, create-pr, docs-writer, github-actions-templates, mcp-builder, dependency-upgrade, catalog-porter, doc-coauthoring, git-master) access sensitive data while exfiltrators (github-actions-templates) send data to external destinations. This pattern may indicate a coordinated attack.
  
  **Remediation:** Review these skills together to ensure they are not collaborating to exfiltrate sensitive data. Consider disabling one or both skills.

  #### [MEDIUM] Multiple skills reference the same external domain
  
  **Severity:** MEDIUM
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_SHARED_URL
  **Location:** (cross-skill analysis)
  
  **Description:** Domain 'app.example.com' is referenced by 2 skills: gitlab-ci-patterns, github-actions-templates. Multiple skills pointing to the same external resource may indicate coordinated C2 or exfiltration.
  
  **Remediation:** Review why multiple skills reference this domain and ensure it is a legitimate, trusted resource.

  #### [MEDIUM] Multiple skills reference the same external domain
  
  **Severity:** MEDIUM
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_SHARED_URL
  **Location:** (cross-skill analysis)
  
  **Description:** Domain 'api.example.com' is referenced by 2 skills: javascript-testing-patterns, mcp-builder. Multiple skills pointing to the same external resource may indicate coordinated C2 or exfiltration.
  
  **Remediation:** Review why multiple skills reference this domain and ensure it is a legitimate, trusted resource.

  #### [MEDIUM] Multiple skills reference the same external domain
  
  **Severity:** MEDIUM
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_SHARED_URL
  **Location:** (cross-skill analysis)
  
  **Description:** Domain 'example.com' is referenced by 2 skills: javascript-testing-patterns, mcp-builder. Multiple skills pointing to the same external resource may indicate coordinated C2 or exfiltration.
  
  **Remediation:** Review why multiple skills reference this domain and ensure it is a legitimate, trusted resource.

  #### [LOW] Skills have complementary descriptions
  
  **Severity:** LOW
  **Category:** social_engineering
  **Rule ID:** CROSS_SKILL_COMPLEMENTARY_TRIGGERS
  **Location:** (cross-skill analysis)
  
  **Description:** Skill 'find-skills' (collector) and 'doc-coauthoring' (sender) have complementary descriptions with shared context: users, this, user, helps, when. This may be intentional design or could indicate coordinated behavior.
  
  **Remediation:** Review these skills to ensure they are not designed to work together maliciously

  #### [LOW] Skills have complementary descriptions
  
  **Severity:** LOW
  **Category:** social_engineering
  **Rule ID:** CROSS_SKILL_COMPLEMENTARY_TRIGGERS
  **Location:** (cross-skill analysis)
  
  **Description:** Skill 'git-master' (collector) and 'doc-coauthoring' (sender) have complementary descriptions with shared context: when, use, context. This may be intentional design or could indicate coordinated behavior.
  
  **Remediation:** Review these skills to ensure they are not designed to work together maliciously
